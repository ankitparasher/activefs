{
  "name": "@activefs/example-remote-tree-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@activefs/source-http": "workspace:*",
    "@activefs/testing": "workspace:*"
  }
}
