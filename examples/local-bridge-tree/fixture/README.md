# Local Bridge Fixture

This folder is served by the local bridge Source API server.

ActiveFS can list and read these files through a different runtime without
copying the fixture into that runtime.
